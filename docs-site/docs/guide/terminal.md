# 终端

AiCode 内置一个完整的终端。本地模式下它连着手机上的 Linux 容器，远程模式下它连着你的 SSH 服务器——两种情况都是真实的 shell，可以装包、编译、跑服务。

从聊天页顶部标题栏的「终端」进入。

## 标签管理

终端支持多个标签同时存在，顶部标签栏可以横向滑动，切换时当前标签会自动滚动到可见位置。

- 标签名前的圆点表示状态：绿色是运行中，灰色是已结束。
- 点标签右侧的 × 关闭，点标签栏末尾的 + 新建。关掉当前标签后会自动切到相邻的一个。
- AI 执行命令时开的终端也会出现在这里，你可以随时切过去看它在干什么。

## 输入与复制

点一下终端区域获得焦点，就可以直接输入。长按可以选择并复制输出内容。

## 辅助按键栏

手机键盘没有 Esc、Ctrl 和方向键，所以终端底部提供了一条可横向滑动的辅助按键栏：

- **控制键**：`ESC`、`TAB`、`CTRL`、`ALT`。Ctrl 和 Alt 按下后会高亮，再按字母键即组成组合键。
- **方向键**：上下左右，长按可以连续发送，用来快速移动光标。
- **常用组合**：`C-c` 中断当前命令、`C-d` 发送 EOF 或退出、`C-z` 挂起进程、`C-l` 清屏。
- **常用符号**：`|`、`~`、`/`、`-`、`_`、`>`、`:`、`$`、`"`、`'` 等，省去切换符号键盘。

## 外观设置

点终端页右上角的齿轮图标（或者从「设置 → 外观与语言 → 终端设置」进入）可以调整：

- **配色主题**：内置 Termius Dark、Dracula、One Dark、Monokai、GitHub Dark 五套配色，选中立即生效，所有已打开的标签一起变。
- **字体大小**：10 到 22 之间无级调节，面板顶部有实时预览。
- **字体**（**1.11.0 起**）：默认是 JetBrains Mono（无连字版本），`0` 和 `O`、`1` 和 `l` 区分得很清楚。也可以切换成系统等宽字体，或者点「导入」从手机里选一个 `ttf` / `otf` / `ttc` 字体文件；导入的字体会出现在列表里，点字体标签上的 × 可以删除，删掉正在用的会自动回落到系统等宽。中文字符始终由系统字体补齐。字体只影响终端，不改变 AI 对话区。
- **光标样式**：方块、下划线、竖线三种。

## Web 服务预览

在容器里跑起 Web 服务（如 `python -m http.server 3000`、`npm run dev`）后，点终端页右上角的**预览**图标，输入服务端口，AiCode 会内置一个全屏浏览器打开 `http://127.0.0.1:端口`，直接在手机上查看效果。

- 预览页顶栏可以**重载**，或点右上角图标**转去系统浏览器**打开。
- 这是本地容器模式的专属功能；远程 SSH 模式下的服务不在这台手机上，点预览会提示不可用。
